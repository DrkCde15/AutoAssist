{
  "name": "autoassist-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "AutoAssist IA - Frontend",
  "scripts": {
    "test": "npx playwright test",
    "test:auth": "npx playwright test --grep \"auth\"",
    "test:unit": "node tests/unit/security-utils.test.js",
    "lint": "eslint public/static/js/",
    "typecheck": "npx tsc --noEmit --allowJs --checkJs public/static/js/*.js",
    "build": "echo \"No build step needed - vanilla HTML/JS/CSS\""
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.0"
  }
}
